# Independent retained legacy2D review

The final3ded891 implementation passes220/220 unchanged independent cases in both Debug and Release. The same compiled harness passes112/220 against frozen4891949:24 failures represent previously unsupported empty/singleton chains,72 represent the previous acceptance of partial parent dummy vectors, and12 expose the incomplete point emitted when an entirely omitted elevation is edited.

All372 final output DXFs per configuration pass ezdxf1.4.4 structural audit with no errors, repairs or removed live entities. The audit also checks explicit-zero versus absent width fields, degenerate vertex counts/planes, and edited parent elevation. Its228 earlier outputs audit structurally clean, while the12 edited elevation controls independently read0 instead of7. The final controls read7 in both transports and10 after a subsequent translation.

The probe loads pinned source files directly and does not reuse owner test fixture factories. It checks native R2000 packet transplants separately from12 six-profile ezdxf producer files and the explicitly declared degenerate/sparse variants. It does not execute native CAD. The original immutable before/final harness sources, results, selected independent observations and972 actual DXF packets are pinned in review.json and output-packets-manifest.json.

The parent entity can receive a new handle after detach/readoption under the existing contract. The audit uses the preserved actual SEQEND identity for that case. Initial audit setup mistakes are disclosed in the receipt and excluded from the final qualification counts.
