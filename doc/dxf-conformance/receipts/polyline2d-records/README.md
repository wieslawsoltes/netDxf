# Legacy 2D record qualification

Production source and complete in-repository qualification are frozen at `3ded8912b17eaea2177a7db5020e2b9933c6a693`. Both Debug and Release pass **3,186 cases each**: 280 new legacy cases, 2,578 related child-record/grammar/topology cases and 328 lightweight-polyline controls. Eight independent raw/geometry gates per configuration validate **832 positive DXF outputs**, **922 corruption controls** and **2,448 reversed lightweight width cross-sections**, with zero audit errors or repairs.

The source evidence is two genuine R2000 native chains and a separate twelve-file unchanged ezdxf 1.4.4 producer matrix. The native source, declared carrier mapping, manifests and reproducible generators are in `tests/fixtures/polyline2d-records`. Empty and singleton chains, source ownership, optional widths, explicit-zero raw variants, reversal, transforms, metadata dependencies, sparse headers and admission boundaries are covered. See `doc/dxf-conformance/polyline2d-records.md` for the exact API and scope.

`production-source-hashes.json` binds all production files. `qualification.json` binds the actual loaded library hashes, results and gate transcripts. Per-configuration output manifests include all generated DXFs; sibling directories also contain twelve deliberately malformed XData-tail inputs, which are negative reader controls rather than positive gate outputs. The build transcripts contain existing CS1591 documentation warnings and no build errors.

`header-before` and `header-after` preserve the exact same compiled standalone harness and both actual output files for the sparse-parent elevation defect. Before source `88fe8f7`, setting Elevation=7 emitted only group30, so independent ezdxf read0. After the fixed source, the same harness with only its loaded library replaced writes the complete10/20/30 point and ezdxf reads7. The two transport conformance cases also prove unchanged wholly omitted points and safe rejection of partial source vectors. The original harness/project text is preserved unchanged in `header-before`; its absolute fixture and library paths document the run environment. To reproduce elsewhere, change those paths only, build against the selected recorded revision, run it, and inspect the parent3D's elevation with ezdxf1.4.4.

**Independent final review is complete.** The unchanged compiled independent harness passes all **220 cases in each configuration** against the exact final libraries. Its external audit checks **372 output drawings per configuration** with zero errors, repairs or removed entities. The same harness on candidate `4891949` passes its original 112 cases and fails the 108 added cases: 24 previously unsupported empty/singleton chains, 72 previously accepted partial dummy points, and 12 incomplete edited omitted points. The independent scalar checks reproduce old elevation 0 where 7 was requested, then read final elevation 7 and translated elevation 10. No remaining production defect was found.

The complete independent package is copied unchanged into `independent/`: original review, three C# harness sources and project, audit script, all before/Debug/Release results and audits, plus the 972 actual output drawings in a compressed archive and its manifest. `independent/review.json` records exact source/library/harness hashes, native versus producer evidence, the baseline failure breakdown, and the audit setup corrections. The earlier output audit is structurally clean despite its demonstrated wrong elevation; scalar and packet assertions supply the corresponding behavioral proof.

Reproduce the owned qualification by building the conformance project in Debug and Release, then running separate `DXF_TEST_FILTER=legacy2d-records`, `poly` and `lw-fidelity` invocations with distinct `DXF_TEST_ARTIFACTS` directories. Run `verify_polyline2d_records.py` on the first directory, the six child-family gates listed in the JSON on the second, and `verify_lwpolyline_fidelity.py` on the third. All commands use the pinned .NET8 SDK and ezdxf1.4.4 fixture/gate environment.
